% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_mapbiomas.R
\name{download_mapbiomas}
\alias{download_mapbiomas}
\title{Download MapBiomas data}
\usage{
download_mapbiomas(
  dest_dir,
  data_type = "cover",
  collection = 10,
  time_range = c(1985, 2024),
  quiet = FALSE
)
}
\arguments{
\item{dest_dir}{Directory where files will be saved. Must be provided by user.}

\item{data_type}{Type of data to download. Currently supports "cover" (land cover classification).
Default is "cover".}

\item{collection}{Collection version. Default is 10.}

\item{time_range}{Vector with start and end years. Default is c(1985, 2024).}

\item{quiet}{Logical. If TRUE, suppresses progress messages. Default is FALSE.}
}
\value{
Invisible vector of downloaded file paths
}
\description{
This function downloads MapBiomas land use and land cover data for specified parameters.
It supports different data types, collections, and time ranges.
}
\details{
The function downloads land cover classification data from MapBiomas Brasil collection.
It automatically creates the destination directory if it doesn't exist and skips existing files.
Currently only supports collection 10 and cover data type.
}
\examples{
\dontrun{
# Download cover data for collection 10, years 1985-2024 to specific directory
download_mapbiomas(dest_dir = "~/mapbiomas_data")

# Download only for specific years
download_mapbiomas(dest_dir = "~/meus_dados", time_range = c(2020, 2022))

# Download with silent mode
download_mapbiomas(dest_dir = "~/mapbiomas", quiet = TRUE)
}
}
\seealso{
Useful links:
\itemize{
  \item \url{https://mapbiomas.org/}
  \item \url{https://mapbiomas.org/en/download}
}
}
